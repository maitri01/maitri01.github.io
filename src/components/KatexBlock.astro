---
import katex from "katex";

interface Props {
  expression: string;
  displayMode?: boolean;
}

const { expression, displayMode = true } = Astro.props as Props;
const html = katex.renderToString(expression, {
  throwOnError: false,
  displayMode
});
---

<div class="katex-wrap" set:html={html} />
